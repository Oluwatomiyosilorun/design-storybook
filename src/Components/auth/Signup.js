import React from 'react';
import { ModalWrapper } from '../Modal'

const Signup = () => {
    return (
        <ModalWrapper>
            <h1>Ican</h1>
        </ModalWrapper>
    );
};

export default Signup;